# Implementation Plans

Generated by the improve skill on 2026-07-14 (perf audit: “app feels painfully slow”).  
Planned against commit `5b6e84a`.

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add week-scoped index on `player_scores` | P1 | S | — | DONE |
| 002 | Server-filter rankings and league players queries | P1 | M | — (benefits from 001) | DONE |
| 003 | Trim `stats` keys on ranked player client DTOs | P1 | M | ideally after 002 | DONE |
| 004 | Scope My Team score loads to roster/watchlist IDs | P1 | M | — | DONE |
| 005 | Unblock league layout + add loading UI | P1 | M | — | DONE |
| 006 | Fix draft polling storms and full-page refresh | P1 | M | stronger after 002/003 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Follow-ups (not in this batch)

- **Live win probability**: Schedule “Chance” column uses projection + linear time-remaining × projection while games are live (`lib/leagues/win-probability`). **Return once live scoring feeds are trusted** — calibrate position σ, add pace blending, DNP/out detection, and OT handling against real in-game data. Search `TODO(live-win-prob)`.

## Dependency notes

- **001** first when possible — index helps every scoreboard query; no code coupling.
- **002 → 003** — filtering first makes payload trim easier to verify; 003 can ship alone.
- **004** independent — adds `playerIds` to `getRankedPlayers`; do not pass `playerIds` from rankings/draft.
- **005** independent — navigation feel.
- **006** after 002/003 if draft refresh remains — smaller pool makes residual refreshes cheaper.

Parallelism OK: `001 ∥ 005`, `004 ∥ 002`, then `003`, then `006`.

## Verify (repo)

```bash
pnpm lint
pnpm exec tsc --noEmit
pnpm test
```

Note: a pre-existing `tsc` error in `lib/queries/players.ts` stats typing may already fail typecheck — do not expand scope to fix unless your change worsens it.

## Findings considered and rejected (this audit)

- **Double middleware + RSC `getUser`**: real but lower leverage than data pages; skip unless still slow after 001–006.
- **`revalidateSettingsPaths` shotgun**: settings-save only; not “every nav.”
- **DIRECT_URL vs pooler**: MED confidence env flip; verify separately before planning.
- **Recharts in bundle**: unused on live routes — drop dependency later as DX, not perf.
- **Supabase Realtime draft**: documented direction; not this batch (006 is the bridge).

## Audit findings mapped

| Audit # | Plan |
|---------|------|
| 6 (index) | 001 |
| 1 (server filter) | 002 |
| 5 (trim stats) | 003 |
| 2 (team ×3 loads) | 004 |
| 3 (layout waterfall) | 005 |
| 4 (draft poll/refresh) | 006 |
